# HMD-AMP — 方式 A:conda + Python 3.8(与原论文 README 完全一致的版本组合)
# 说明:
#   - deep-forest==0.1.7 在 PyPI 上有 cp38 预编译 wheel,无需编译
#   - torch 2.4.1 是最后一个支持 Python 3.8 的版本;PyPI wheel 自带 CUDA 12.1 运行时,
#     无 GPU 的 CPU 机器同样可用;WSL2 + NVIDIA 显卡(装好 Windows 侧驱动)可直接用 GPU
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
numpy==1.19.5
pandas==1.2.0
scikit-learn==1.3.0
biopython==1.83
fair-esm==2.0.0
deep-forest==0.1.7
