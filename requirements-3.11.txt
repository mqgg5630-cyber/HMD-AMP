# HMD-AMP — 方式 B:venv + Python 3.9~3.11(推荐 3.11,已验证)
# 说明:
#   - numpy/pandas 不能用 README 里的 1.19.5/1.2.0(没有 3.9+ 的安装包),
#     这里换成功能等效的最新 1.x 版本
#   - deep-forest 由 install.sh 从 LAMDA-NJU 官方源码编译安装,不在此文件中
#   - torch 从 PyPI 安装(PyPI wheel 自带 CUDA 运行时,CPU 机器也可用)
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
numpy==1.26.4
pandas==1.5.3
scikit-learn==1.3.0
biopython==1.83
fair-esm==2.0.0
cython==0.29.37
